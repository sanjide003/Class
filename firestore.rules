rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: authentication is intentionally disabled while the app is built.
    // Replace these rules before sharing the public GitHub Pages links.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
